export class GameFilters {
    name: string | null = '';
    minPlayers: number | null = 0;
    maxPlayers: number | null = 0;
    playingTime: number | null = 0;
    mechanics: string[] = [];
    genres: string[] = [];
}
